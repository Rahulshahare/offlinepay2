import 'package:flutter/material.dart';

class UpiPinScreen extends StatelessWidget {
  const UpiPinScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('UpiPinScreen')));
}
